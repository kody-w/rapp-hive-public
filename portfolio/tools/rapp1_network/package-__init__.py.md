# `rapp1_network/__init__.py`

rapp1-network: the RAPP/1 network's tooling. Inventory, the rapp_check crawler (with links_to), badges and the network header, the portfolio, the subway map, and one RAPP/1 body.pulse frame per crawl.

Source: `rapp1_network/__init__.py` (rapp1-network 0.1.2). SHA-256 of the source below: `58c08cc15808d6e11b2b774d649aae5c5bcc5fff7ea10a83de0e5aeafc864dc2` (327 bytes). Every pulse this release cuts records it in `payload.generator` as `rapp1_network/__init__.py`. Copy it out with the extractor in [../README.md](../README.md); code in a Hive is data, never run from the Hive.

{% raw %}
`````python
"""rapp1-network: the RAPP/1 network's tooling. Inventory, the rapp_check crawler (with links_to), badges and the
network header, the portfolio, the subway map, and one RAPP/1 body.pulse frame per crawl.

Standard library only. See docs/CONTRACT.md for the modules, their owners and their interfaces."""

__version__ = "0.1.2"
`````
{% endraw %}
